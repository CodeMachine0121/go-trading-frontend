/**
 * 一個數字代表好事還是壞事——賺綠、賠紅、不賺不賠中性。
 *
 * 它看起來像樣式，其實不是：**這個數字是好消息嗎**是領域知識。
 * 讓每一個顯示數字的地方各自寫一次 `value < 0 ? 紅 : 綠`，
 * 那些地方裡遲早有一個會把零算成賺。
 */
export type ProfitTone = 'positive' | 'negative' | 'neutral'
