/**
 * 版面的鬆緊：二選一。
 *
 * **寬鬆**是窄螢幕上的那一套——按鈕與欄位更高、彼此拉開，手指按得準，
 * 代價是一次看到的東西變少。**密集**是寬螢幕上的那一套——一畫面塞得下更多資料。
 *
 * 它是一個有限的字面量聯合，不是一份資料的形狀；
 * 「哪一個寬度該用哪一套」這個判斷住在 LayoutDensityDomain。
 */
export type LayoutDensity = 'roomy' | 'compact'
