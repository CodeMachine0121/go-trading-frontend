/**
 * 登入畫面現在在做哪一件事。有限的字面量聯合，是規範允許使用 `type` 的唯一情形——
 * 它描述的是一組固定取值，不是一份資料的形狀。
 *
 * 它是一個**值**而不是兩個畫面，因為兩件事填的是同樣的兩格；
 * 而它必須被明講出來，是因為**送出前的長度規則只在建立帳號時套用**——
 * 沒有這個值，那條規則就只能靠呼叫端自己記得要不要跳過。
 */
export type SignInMode = 'signIn' | 'register'
