# 後端 go-trading REST API base URL
NUXT_PUBLIC_BACKEND_BASE_URL=http://localhost:8080

# 本站的 origin 必須列在後端的 CORS_ALLOWED_ORIGINS 內，瀏覽器才讀得到回應。
# 開發時預設是 http://localhost:3000（見 nuxt.config.ts 的 devServer.port）。
