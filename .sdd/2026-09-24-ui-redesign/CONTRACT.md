# Contract Traceability Matrix — 2026-09-24-ui-redesign（操作台全面改版）

Contract: PRD.md (v1.0, Finalized；re-read after the edit that renamed and reworded US-03 「刪掉之前先說有誰在用它」, added 「引用的訊號來源被刪掉」, changed the §4 導覽 rule and changed the §5 頂列 wording)
Design map: ARCH.md (§7 Traceability)
Glossary: .sdd/UL-MAP.md
Implementation: `app/`（tests in `tests/`, mirroring `app/`）
Oracle: Acceptance Criteria — 40 clauses (29 AC · 8 BR · 3 NFR；NFR Analytics = N/A, skipped)
Audit date: 2026-09-24 (re-run on `chore/ui-redesign-reset` @ 9f73152) · Method: static conformance audit. For every clause that was new, changed, or not ✅ in the previous run, the oracle was derived from the current PRD text first; then the test and the code were audited separately against it. Clauses that were ✅ before and whose PRD text did not change are carried over unchanged. Three mapped specs were run singly as corroboration, with `--maxWorkers=1`: `MarketSwitch.spec.ts` 6/6, `TradingStrategyWorkbench.spec.ts` 64/64, `KCandleSearchPanel.spec.ts` 30/30. The full suite was not run.

ID stability: AC-1…AC-28, BR-1…BR-8 and NFR-1…NFR-3 keep their previous IDs. AC-18 kept its ID; its scenario was renamed and reworded. The new scenario 「引用的訊號來源被刪掉」 was appended as **AC-29**. BR-3's text changed. The three US-01 scenarios 「切過一次…」「重新整理後…」「不分兩邊的畫面不改變…」 were already AC-5/6/7, and their text did not change.

Out-of-Scope negative checklist (PRD §1): new business capabilities/rules · drag-to-customize layout / saved layouts · any change to the trading service.

## Clauses

`Spec-expected` is the business-observable oracle from Phase 2. The audit columns check the concrete artifact it bridges to, using UL-MAP and ARCH.

| ID | Clause | Spec-expected (oracle) | Impl | Test | Test audit | Code audit | Status |
|----|--------|------------------------|------|------|------------|------------|--------|
| AC-1 | 從現貨行情圖表切到合約：Given 使用者在現貨 K 線圖表 / When 打開現貨／合約開關 / Then 畫面換成合約 K 線圖表 And 開關停在「合約」 | 所在畫面變為合約 K 線圖表；開關顯示「合約」 | market-counterpart-domain.ts:9-46；MarketSwitch.vue:15-22；layouts/console.vue:23,43-47 | market-counterpart-application.spec.ts:22；MarketSwitch.spec.ts:11-29 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-2 | 從合約機器人清單切回現貨：Given 合約策略機器人清單 / When 關上開關 / Then 現貨策略機器人清單 And 開關停在「現貨」 | 變為現貨機器人清單；開關顯示「現貨」 | market-counterpart-domain.ts:13,39-40；MarketSwitch.vue:15-22 | market-counterpart-application.spec.ts:28；MarketSwitch.spec.ts:13,24 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-3 | 編輯機器人時切到另一邊，去的是清單：Given 正在編輯一台現貨機器人 / When 打開開關 / Then 合約策略機器人清單 | 到達合約機器人**清單**（不是合約的編輯或建立畫面） | market-counterpart-domain.ts:36-40 | market-counterpart-application.spec.ts:41-52 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-4 | 從網址直接進入合約畫面：Given 直接打開合約 K 線瀏覽的網址 / When 畫面出現 / Then 開關已經停在「合約」 | 第一次呈現時開關就顯示「合約」 | layouts/console.vue:23（由 route.path 算 DTO）；market-counterpart-dto.ts:20-22 | market-counterpart-application.spec.ts:25；MarketSwitch.spec.ts:22-29 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-5 | 切過一次，其他有兩邊的畫面都帶著走：Given 在現貨 K 線圖表打開開關到合約 K 線圖表 / When 從導覽點「策略腳本」 / Then 合約策略腳本 And 開關停在「合約」 | 到達合約策略腳本；開關顯示「合約」 | use-market-side.ts:16-26；layouts/console.vue:26-31；ConsoleLayout.vue:64-66 | use-market-side.spec.ts:25-33；ConsoleLayout.spec.ts:104-110 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-6 | 重新整理後仍記得上次那一邊：Given 上次停在合約 / When 重新整理並從導覽點「策略機器人」 / Then 合約策略機器人 | 重新整理後，「策略機器人」導向合約機器人 | use-market-side.ts:14；market-side-domain.ts:14-16；market-side-preference-proxy.ts | use-market-side.spec.ts:43-47；market-counterpart-application.spec.ts:72-100；market-side-preference-proxy.spec.ts | asserts-oracle | produces-oracle | ✅ conforms |
| AC-7 | 不分兩邊的畫面不改變記住的那一邊：Given 上次停在合約 / When 去了交易策略再點「K 線資料」 / Then 合約 K 線瀏覽 | 去過不分兩邊的畫面後，記住的那一邊不變 | use-market-side.ts:19 | use-market-side.spec.ts:35-41 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-8 | 不分現貨合約的畫面：Given 在交易策略畫面 / When 想按開關 / Then 開關按不動 And 畫面說明「這個畫面不分現貨與合約」 | 開關按不動，**而且畫面上看得到**「這個畫面不分現貨與合約」 | market-counterpart-domain.ts:17,45（不分兩邊時 `switchable=false`＋該句）；MarketSwitch.vue:29,41-46（按不動，並在開關下方以常駐文字畫出那一句，不靠滑鼠停留提示）；ConsoleLayout.vue:190-192（market slot 寬窄螢幕都畫） | MarketSwitch.spec.ts:31-40（按下不 emit、`aria-disabled`、`market-switch-reason` 文字一字不差）；MarketSwitch.spec.ts:42-46（切得動時不出現）；market-counterpart-application.spec.ts:54-69（`/trading-strategies` 得到 `switchable=false` 與該句） | asserts-oracle | produces-oracle | ✅ conforms（corroborated: MarketSwitch.spec.ts 6/6） |
| AC-9 | 沒選過時跟隨系統：Given 從沒選過 And 系統深色 / When 打開操作台 / Then 操作台是深色 | 呈深色 | appearance-domain.ts:25,34-38；use-appearance.ts:32-33；app.vue:17-18 | use-appearance.spec.ts:45-51；appearance-application.spec.ts:20 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-10 | 選過的外觀重新整理後仍在：Given 選了淺色 / When 重新整理 / Then 仍是淺色 | 重新整理後仍是淺色 | appearance-service.ts:18-23；appearance-preference-proxy.ts:6-23 | appearance-preference-proxy.spec.ts:10-16；appearance-application.spec.ts:22,47 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-11 | 跟隨系統時系統切換：Given 選跟隨系統 And 系統深色 / When 系統切換成淺色 / Then 當下換成淺色，不必重新整理 | 系統一換立即變淺色，不重新整理 | use-appearance.ts:40-44 | use-appearance.spec.ts:53-62 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-12 | 手動選擇不受系統影響：Given 選深色 / When 系統換淺色 / Then 維持深色 | 維持深色 | appearance-domain.ts:38 | use-appearance.spec.ts:64-71；appearance-application.spec.ts:63 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-13 | 瀏覽器記不住：Given 記不住任何東西 / When 選了淺色 / Then 換成淺色 And 關掉再打開時回到跟隨系統 | 立即變淺色；下次打開回到跟隨系統 | appearance-preference-proxy.ts:6-23；use-appearance.ts:47-49 | appearance-preference-proxy.spec.ts:18-31；use-appearance.spec.ts:73-82；appearance-application.spec.ts:20 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-14 | 兩個入口一致：Given 在頂列選了深色 / When 打開設定畫面 / Then 外觀那一項顯示「深色」 | 設定畫面的外觀項顯示「深色」 | use-appearance.ts:16（共用 useState）；pages/settings/index.vue:128-134；AppearanceToggle.vue:61-67 | settings-display-section.spec.ts:48-52；use-appearance.spec.ts:73-82 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-15 | 在步驟卡上加一條買入條件：Given 兩個訊號來源「突破」「動能」 / When 在「什麼算買入」加「突破 等於 買入」 / Then 那張卡上出現它 | 「什麼算買入」卡顯示「突破 等於 買入」 | use-trading-strategy-form.ts:391-393；TradingStrategyWorkbench.vue:296-318 | TradingStrategyWorkbench.spec.ts:184-191 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-16 | 手機上可以編：Given 手機直立打開一份交易策略 / When 在「什麼算賣出」卡刪掉一條 / Then 那一條消失，並提示有還沒存的改動 | 那一條消失，**而且畫面上出現有未存改動的提示** | 刪除：use-trading-strategy-form.ts（條件板編輯行為）；未存提示：TradingStrategyWorkbench.vue:115-116（`dirty` 跟上次存下的那一份比）、:209-214（「還沒存的改動」常駐在儲存鍵旁） | TradingStrategyWorkbench.spec.ts:237-252（`onAPhone()` 下刪掉「動能」後只剩 `item-sell-突破`，且 `trading-strategy-unsaved` 讀成「還沒存的改動」）；同檔「存好之後」一案斷言提示消失 | asserts-oracle | produces-oracle | ✅ conforms（corroborated: TradingStrategyWorkbench.spec.ts 64/64） |
| AC-17 | 把兩條扣成一組：Given 買入卡有兩條 / When 扣成一組並選「或」 / Then 讀成「突破 等於 買入 或 動能 等於 買入」 | 卡讀成那一整句 | condition-board-domain.ts；ConditionClauseSentence.vue；use-trading-strategy-form.ts:407-409 | TradingStrategyWorkbench.spec.ts:302-316 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-18 | 刪掉之前先說有誰在用它：Given 「什麼算買入」上有一條引用「動能」 / When 使用者打開訊號來源卡上「動能」的設定 / Then 設定裡說出「條件裡還在用「動能」，刪掉它會一併拿掉那幾句條件」 | 打開「動能」的設定後，設定裡出現那一句（一字不差，名字是被引用的那一個） | use-trading-strategy-form.ts:132-146（依兩張條件卡實際引用的代號組句；:141 一字不差）；TradingStrategySignalSourceCard.vue:329-335（畫在該來源的設定裡） | TradingStrategyWorkbench.spec.ts:417-425（打開 index 0 的設定，`toBe('條件裡還在用「MACD」，刪掉它會一併拿掉那幾句條件')`；預設 fixture 的買入條件引用 MACD） | asserts-oracle | produces-oracle | ✅ conforms（PRD 用詞已改為「打開設定」。測試以 MACD 代入同一情境） |
| AC-19 | 正常存好：Given 開盤 100、最高 110、最低 95、收盤 105 / When 存 / Then 存好，表格上那一根換成新數字 | 存成功，表格那一根顯示新的數字 | KCandleEditorPanel.vue:65,296-306（存）；KCandleSearchPanel.vue:266（`changed` → 重查） | KCandleSearchPanel.spec.ts:354-392（原本 100/120/90/110，改成 100/110/95/105 後存，只送出一次，表格那一列第三到第六格讀成 `['100','110','95','105']`） | asserts-oracle | produces-oracle | ✅ conforms（corroborated: KCandleSearchPanel.spec.ts 30/30。新數字來自重查的回應，和實際行為走同一條路） |
| AC-20 | 最高價等於最低價：Given 最高 100、最低 100 / When 存 / Then 存好 | 存成功（相等不被擋） | k-candle-write-domain.ts:87-89（`lessThan`） | KCandleEditorPanel.spec.ts:214-227 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-21 | 最高價低於最低價：Given 最高 90、最低 95 / When 看表單 / Then 最高價那一格標紅，說出「最高價不得低於最低價」 And 儲存按不下去 | 最高價欄標為錯誤＋該句；儲存不可按 | k-candle-write-domain.ts:87-89；KCandleEditorPanel.vue:65-97；KCandleForm.vue:172-178,201 | KCandleEditorPanel.spec.ts:165-186 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-22 | 身分欄位改不了：Given 打開已存在的 K 線 / When 看交易標的與起始時間 / Then 兩格唯讀，並說明要改請刪掉重建 | 兩格唯讀＋「要改請刪掉重建」說明 | KCandleForm.vue:48,125,140,149-151 | KCandleEditorPanel.spec.ts:231-240,258-262 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-23 | 行情圖表的即時跟盤照舊：Given 現貨 K 線圖表看一檔有即時更新的標的 / When 市場跳動 / Then 最後一根跟著動，行情摘要的價格同步更新 | 最後一根更新，**而且**行情摘要價格同步 | KCandleChartPanel.vue:353,455-458；k-candle-chart-dto.ts:61-63（摘要取最後一根）；KCandleQuote.vue:63-87 | KCandleChartPanelLiveUpdates.spec.ts:112-124（跳動前摘要是 110、沒有 118；跳動後最後一根收 118，且 `k-candle-quote` 讀到 118） | asserts-oracle | produces-oracle | ✅ conforms |
| AC-24 | 合約行情摘要多了標記價格：Given 合約 K 線圖表 / When 畫面出現 / Then 看得到標記價格、指數價格與溢價指數 And 沒有記錄的畫成「—」而不是零 | 三項都在；缺值畫「—」不是 0 | KCandleContractChartPanel.vue:303-326 | KCandleContractChartPanel.spec.ts:378-397 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-25 | 回測結果出現在編輯器下方：Given 策略腳本畫面寫好「一個信號」算式 / When 按執行回測 / Then 成績單、資金曲線與交易明細出現在編輯器下方，不換頁 | 三者一起出現在編輯器下方，不換頁 | IndicatorCalculationPanel.vue:~960（`outcome` 區內的 StrategyScriptBacktestPane，用 v-show 切換） | IndicatorCalculationPanelDestinations.spec.ts:105-120；StrategyScriptBacktestPane.spec.ts:367 | asserts-oracle | produces-oracle | ✅ conforms |
| AC-26 | 需要處理的那一輪說出原因：Given 一台機器人最近一輪買入與賣出同時成立（規則打架） / When 使用者看它的執行紀錄 / Then 那一輪標成需要處理，並附上建議的下一步 | 那一輪標成需要處理，且出現完整的下一步建議 | strategy-bot-run-record-domain.ts:27（衝突 → needsAttention + suggestionText）；StrategyBotRunHistory.vue:102-108 | StrategyBotRunHistory.spec.ts（needs-attention 案例，整句 `toBe('買入與賣出同時成立，在改掉其中一邊之前它不會說話')`） | asserts-oracle | produces-oracle | ✅ conforms — PRD 的 Given 已改成與 UL-MAP 一致的「規則打架」（它不是停擺） |
| AC-27 | 手機上叫出助手：Given 手機直立 / When 按底部「助手」 / Then 助手以整頁出現，接著的是同一段對話 | 整頁助手，接續同一段對話 | ConsoleLayout.vue:33（底部「助手」→ `/chat`）；pages/chat/index.vue:8-31（整頁、`consoleFillsViewport`、取用共用對話）；use-assistant-conversation.ts:61-76（`useState` 共用 id／訊息／草稿）；app.vue:44（抽屜取用同一份） | ConsoleLayout.spec.ts:133-140（手機寬度下「助手」一格 `href='/chat'`）；use-assistant-conversation.spec.ts:167-189（在抽屜那一份問過，整頁那一份看得到同一個 id 與同一串問答；草稿也共用） | asserts-oracle | produces-oracle | ✅ conforms |
| AC-28 | 連不上後端：Given 交易服務沒有回應 / When 打開任何一個畫面 / Then 畫面明說連不上，而不是一片空白 | 每個畫面都明說連不上 | backend-unreachable-error.ts:15-17（`explanation`）；use-strategy-bots.ts:182-184、use-strategy-bot-workbench.ts:115-117、use-trading-strategies.ts:81-83、use-trading-strategy-workbench.ts:227-229 先分流 `BackendUnreachableError`；圖表、K 線、策略腳本、市集、助手、設定、登入原本就有分流 | use-strategy-bots.spec.ts:89-96、use-strategy-bot-workbench.spec.ts:114-121、use-trading-strategies.spec.ts:64-71、use-trading-strategy-workbench.spec.ts:167-174（都丟真的 `BackendUnreachableError`，斷言整句「連不上後端 go-trading API…」） | asserts-oracle | produces-oracle | ✅ conforms（殘留：KCandleChartPanel.vue:211-213，使用者按「補齊」失敗時仍寫「補不回來：backend unreachable: …」。那是按下按鍵之後的結果，不是打開畫面，所以不算違反此條） |
| AC-29 | 引用的訊號來源被刪掉：Given 「什麼算買入」上有「突破 等於 買入」且「動能 等於 買入」 / When 在訊號來源卡刪掉「動能」 / Then 「什麼算買入」只剩「突破 等於 買入」 | 刪掉後，買入卡只讀出「突破 等於 買入」這一句 | use-trading-strategy-form.ts:270-273（刪來源）；use-trading-strategy-form.ts:376 → condition-board-domain.ts:164-169（條件板對齊現存代號，拿掉引用已刪來源的條件，空的一組也一併拿掉） | TradingStrategyWorkbench.spec.ts:405-415（fixture 的買入卡正好兩條；刪掉第二個來源後 `item-buy-動能` 不存在，`item-buy-突破` 讀成「突破 等於 買入」） | asserts-oracle | produces-oracle | ✅ conforms（新增條款。若改成斷言整張卡的條目清單等於 `['item-buy-突破']`，會更直接對上「只剩」） |
| BR-1 | 對應畫面：圖表⇄圖表、瀏覽⇄瀏覽、腳本⇄腳本、現貨機器人（清單、建立、編輯）⇄合約機器人清單；其餘去處沒有對應 | 四組對應；建立或編輯一律到另一邊清單；其餘不可切 | market-counterpart-domain.ts:9-46 | market-counterpart-application.spec.ts:20-70 | asserts-oracle | produces-oracle | ✅ conforms |
| BR-2 | 外觀：淺／深／跟隨系統，預設跟隨系統；記住在這台瀏覽器；記不住時本次仍生效 | 同左 | appearance-domain.ts:25；appearance-preference-proxy.ts | appearance-application.spec.ts；appearance-preference-proxy.spec.ts | asserts-oracle | produces-oracle | ✅ conforms |
| BR-3 | 導覽：寬螢幕側欄七個去處（行情圖表、K 線資料、策略腳本、交易策略、策略機器人、Marketplace、設定），有兩邊的那幾格指向使用者最後停留的那一邊（沒停過時是現貨）；窄螢幕底部五格（行情、策略、機器人、助手、更多） | 側欄恰七項、依序；有兩邊的格子（含底部與「更多」）去最後停留那一邊的畫面，沒停過去現貨；不分兩邊的格子不變；底部恰五格、依序 | ConsoleLayout.vue:17-39,64-66（`pathOf` 照對照指路）；layouts/console.vue:26-31（四個有兩邊的去處交給 `pathOnMarketSide`）；use-market-side.ts:14-26；market-side-domain.ts:14-16（空值或看不懂的值 → 現貨）；market-counterpart-domain.ts:52-58 | ConsoleLayout.spec.ts:57-62（七項依序）、:104-110（照對照指路，交易策略不變）、:112-122（底部與更多也照對照）、:124-131（底部五格依序）；use-market-side.spec.ts:25-55；market-counterpart-application.spec.ts:72-111（沒切過是現貨；各格在兩邊各去哪） | asserts-oracle | produces-oracle | ✅ conforms（PRD 文字已改成「最後停留的那一邊」。ConsoleLayout.vue:14-15 的註解與 ARCH §6「桌面側欄仍只指向現貨那一邊」還是舊說法） |
| BR-4 | 交易策略步驟卡：訊號來源 → 什麼算買入 → 什麼算賣出；群組、且／或規則沿用；改用「挑」不是「拖」；窄螢幕不再唯讀 | 三卡依序；以點選編輯；窄螢幕可編 | TradingStrategyWorkbench.vue:275-320；拖拉相關（use-piece-drag*、interactjs、piece-drag-markup）已移除 | TradingStrategyWorkbench.spec.ts:122-132,236-299 | asserts-oracle | produces-oracle | ✅ conforms |
| BR-5 | 其餘一切業務規則（送出把關、拒絕說法、空清單、未儲存確認、時區）一字不變 | 行為與改版前完全相同 | 整個 application/domain 層（ARCH：Not touched） | 既有 domain/application specs | — | unclear | ❔ unclear — 範圍涵蓋整個舊版行為，靜態審查無法跟改版前逐項比對。另外，策略腳本畫面新增了離開時的確認（見 BR-6）；UL-MAP:143 原本只攔會覆蓋編輯區的動作，這是為了 BR-6 而擴大的行為 |
| BR-6 | 有未儲存的改動時按開關或換畫面 → 照既有的未儲存確認攔下 | 有未存改動時按開關或換畫面，會先被未儲存確認攔下；什麼都沒改時不攔 | layouts/console.vue:46（開關走 `navigateTo`，所以會觸發離開守衛）；pages/trading-strategies/[id].vue:117-118；StrategyBotWorkbenchPage.vue:51-52；pages/strategy-scripts/index.vue:26-28、pages/contract-strategy-scripts/index.vue:27-29 → IndicatorCalculationPanel.vue:316 → use-strategy-script-library.ts:206-208 | trading-strategy-pages.spec.ts:92-121、StrategyBotWorkbenchPage.spec.ts:111-133、strategy-script-page-titles.spec.ts:51-74（都抓下真的離開守衛：沒改過放行、不問；改過會問「這一頁改過的東西還沒存，確定要離開嗎？」，答「不要」就留在原頁；現貨與合約策略腳本都有） | asserts-oracle | produces-oracle | ✅ conforms（備註：從開關到守衛這一段靠 `navigateTo`，沒有測試斷言，見 NFR-1。策略腳本離開時用的是瀏覽器確認框，不是編輯區原本的「捨棄」對話框。use-strategy-script-library.ts:200-210 的新函式插在 `guardOverwritingDraft` 的 JSDoc 與函式之間，那段說明現在掛錯地方） |
| BR-7 | 交易服務沒有回應 → 每個畫面都明說連不上 | 同 AC-28 | 同 AC-28 | 同 AC-28 | asserts-oracle | produces-oracle | ✅ conforms（與 AC-28 同一個修正） |
| BR-8 | 瀏覽器記不住外觀 → 跟隨系統，選了本次生效 | 同 AC-13 | appearance-preference-proxy.ts:6-23 | appearance-preference-proxy.spec.ts:18-31 | asserts-oracle | produces-oracle | ✅ conforms |
| NFR-1 | 切換外觀與現貨／合約開關不重新載入整個操作台 | 兩種切換都不整頁重載 | use-appearance.ts:23-26（只改 `<html data-theme>`）；layouts/console.vue:46（`navigateTo` 走 client-side） | use-appearance.spec.ts:73-82（同一份 document 內切換）；tests/layouts/console.spec.ts（開關的 navigate 走 navigateTo、不呼叫 window.location.reload） | asserts-oracle | produces-oracle | ✅ conforms |
| NFR-2 | 沿用既有登入與開通把關，不新增 | 所有新畫面都經過既有的登入與開通把關 | middleware/signed-in.global.ts（global，涵蓋所有頁） | tests/middleware/signed-in.global.spec.ts | asserts-oracle | produces-oracle | ✅ conforms |
| NFR-3 | 最窄手機直立寬度；橫放；近兩年主流瀏覽器 | 各尺寸與瀏覽器下都可用 | ConsoleLayout.vue／layout-density-domain.ts（768／1024 兩道分界） | —（1899224 修的是對比度，不是這一條） | no-test | unclear | ❔ unclear — 需要實機或瀏覽器驗證，讀程式碼判斷不了 |

## Orphans (code with no clause)

| Code | Description | Verdict |
|------|-------------|---------|
| — | 沒找到落在 Out-of-Scope 的行為：沒有拖拉自訂版面，也沒有存多組版面；沒有改動交易服務 | none |

Notes (not orphans):
- PRD §5 頂列 now reads 「現貨／合約開關、顯示時區、外觀快速切換、助手鍵」. That matches `ConsoleLayout.vue:190-200`, so the earlier 「搜尋」 mismatch is resolved. ARCH §2 still lists 「搜尋」 in the top strip.
- The strategy-script leave confirmation (BR-6) is broader than UL-MAP:143 「未儲存的變更」, which only guards actions that overwrite the editor. UL-MAP should say that leaving the page is guarded too.

## Summary

- Conforms: 38/40 clauses ✅ (95.0%)
- Violations: none (AC-26 resolved: the PRD precondition now says 規則打架, matching UL-MAP and the code)
- Mis-asserted: none
- Partial: none (NFR-1 resolved: tests/layouts/console.spec.ts pins client-side navigation)
- Gaps: none
- Unclear: BR-5, NFR-3 — need a before/after behaviour comparison and real-device checks; not decidable by reading
- Orphans: 0
- Resolved since the previous run: AC-8, AC-16, AC-18, AC-19, AC-23, AC-27, AC-28, BR-6, BR-7. Newly added and conforming: AC-29. Text changed and still conforming: BR-3.
